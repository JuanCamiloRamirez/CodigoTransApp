<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.parcial.codigotransapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CodigoTransApp">
        <activity android:name=".services.CodigoTransitoActivity"></activity>
        <activity android:name=".services.RegistroVehiculoActivity" />
        <activity android:name=".services.ListaVehiculo" />
        <activity android:name=".services.Menu" />
        <activity android:name=".services.SingupActivity" />
        <activity android:name=".services.login.SingInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>